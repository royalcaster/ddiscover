# 2026-06-16 Session Summary

## Club Map Marker Styling

- Time: 13:15-13:17 CEST, approximate duration 2 minutes.
- User asked to make map markers look like a club icon in a circle and explicitly avoid blue.
- Updated native MapLibre markers in `src/components/discover-map.tsx`:
  - replaced the blue default marker with a black circular marker,
  - added a yellow `Music2` club/music icon,
  - kept the selected marker yellow with a black icon.
- Updated the web fallback marker list in `src/components/discover-map.web.tsx` to use the same circular music-icon treatment instead of the generic map pin.
- Validation: `npx tsc --noEmit` passed. npm still prints the existing unknown `python` env config warning.

## Beer Icon Marker Refinement

- Time: 15:09-15:10 CEST, approximate duration 1 minute.
- User asked to use the beer icon for club markers, remove marker borders, use white background with black foreground for default markers, and yellow background with black foreground for the active marker.
- Updated native and web marker renderers to use Lucide `Beer`.
- Validation: `npx tsc --noEmit` passed.

## Theme Toggle Width Fix

- Time: 15:12-15:13 CEST, approximate duration 1 minute.
- User reported that the three `Farbschema` options did not span the full control width.
- Updated `ThemeModeToggle` buttons from fixed minimum widths to equal `flex-1` segments with centered content.
- Validation: `npx tsc --noEmit` passed.

## Discover Map Location And Today Filter

- Time: 15:18-15:29 CEST, approximate duration 11 minutes.
- User asked to show the user's current location on the map, add an icon button to center the map on the user's location, and add an `Open today` filter button.
- Added `expo-location` and configured the foreground location permission string in `app.json`.
- Updated `src/app/(tabs)/index.tsx`:
  - requests foreground location only when the locate button is tapped,
  - stores the user coordinate and camera target,
  - filters club markers to clubs with events on the current local day when the filter is active,
  - filters the selected club event list to today's events while the filter is active.
- Updated `src/components/discover-map.tsx`:
  - renders the user's location as a yellow app-colored circular marker with a user icon,
  - adds a white locate icon button,
  - adds a labeled `Heute offen` filter pill with active yellow state.
- Updated `src/components/discover-map.web.tsx` with matching fallback controls.
- Validation:
  - `npx tsc --noEmit` passed.
  - `package.json`, `bun.lock`, and `package-lock.json` all include `expo-location`.
- Note: because `expo-location` is a native module and `app.json` permissions changed, the Android dev client must be rebuilt before testing this feature in the emulator.

## Discover Map Control Positioning

- Time: 16:36-16:37 CEST, approximate duration 1 minute.
- User asked to place the location icon in the far top-right corner and the `Heute offen` button directly to its left.
- Updated native map controls to render as a horizontal top-right row.
- Validation: `npx tsc --noEmit` passed.

## Discover Bottom Sheet Collapsed Height

- Time: 13:18 CEST, approximate duration 3 minutes.
- User clarified that the `Naechstes Event` container should not peek out when the Discover bottom sheet is collapsed.
- Reduced the collapsed club-summary height in `src/components/discover-bottom-sheet.tsx` so the hidden state exposes only the handle, club badge/count, club name, address, and favorite button.
- Named the collapsed height constants for easier tuning.
- Validation: `npx tsc --noEmit` passed. npm still prints the existing unknown `python` env config warning.

## Discover Club Website Action

- Time: 13:22 CEST, approximate duration 3 minutes.
- User asked to turn the `Clubseite` text chip into an icon-only button beside the club save heart button, with a white foreground.
- Moved the club website action into the bottom sheet header next to the favorite heart and removed the lower `Clubseite` text button.
- User reported the hardcoded dark website button was wrong in light mode, so the icon button now uses the themed secondary surface and foreground color like the adjacent heart button.
- Validation: `npx tsc --noEmit` passed. npm still prints the existing unknown `python` env config warning.

## Android Emulator Performance Tuning

- Time: 15:36-15:55 CEST, approximate duration 19 minutes.
- User reported the Android emulator felt much slower than expected and asked to optimize the emulator and check the host system for performance issues.
- Reviewed prior session summaries and continued from the earlier finding that `ddiscover_dev_device` had previously failed due to low `C:` disk space and a large snapshot/userdata footprint.
- Found stale Android tooling state:
  - A hidden `qemu-system-x86_64` process from 11:31 was still running after `emulator.exe` had disappeared, had accumulated high CPU time, and kept the AVD quickboot RAM snapshot locked.
  - A stale ADB process was stuck in a localhost connection attempt to port `8081`, causing ADB and some cleanup commands to hang.
- Stopped the stale QEMU/emulator backend and reclaimed the `ddiscover_dev_device` quickboot snapshot space:
  - `snapshots/default_boot/ram.img` was about 4 GB.
  - the project AVD shrank from about 8.44 GB to about 4.44 GB.
  - `C:` had about 38.76 GB free after cleanup.
- Tuned the project AVD config at `C:\Users\klaus\.android\avd\ddisover_dev_device.avd\config.ini`:
  - reduced CPU cores from 4 to 2,
  - reduced RAM from 4096 MB to 1536 MB,
  - reduced configured display from 1440x3120 to 1080x2400 at 420 dpi,
  - disabled device frame, emulated cameras, and audio input,
  - kept host GPU acceleration enabled.
  - Saved a backup as `config.ini.pre-ddiscover-performance-tuning.bak`.
- Applied runtime Android display override for the currently running emulator with `adb shell wm size 1080x2400` and `adb shell wm density 420`; the physical emulator skin still reports 1440x3120, but rendering now uses the lower override.
- Updated Android helper scripts:
  - `scripts/android-emulator.js` and `scripts/android-dev.js` now launch emulators with `-no-boot-anim -no-audio -no-snapshot-save -gpu host`.
  - The default emulator launch can be extended with `DDISCOVER_ANDROID_EMULATOR_ARGS`.
  - `scripts/android-emulator.js` now reuses an already running emulator instead of launching another instance.
  - `scripts/android-dev.js` and `scripts/dev-server.js` now use `netstat` for Windows port cleanup instead of `Get-NetTCPConnection`, which was hanging on this system.
  - Emulator stderr is inherited instead of silently discarded, so startup failures are visible.
- Updated `README.md` with a concise note about the lighter emulator launch defaults.
- Validation:
  - `node --check scripts/android-emulator.js scripts/android-dev.js scripts/dev-server.js` passed.
  - `npm run emu:list` passed.
  - `npm run dev:stop:all` passed and no longer hung.
  - `adb devices -l` responded normally.
  - `ddiscover_dev_device` booted and appeared as `emulator-5554 device`.
  - `npm run emu:reverse` enabled `tcp:8081 -> tcp:8081`.
  - After the final restart, the tuned emulator was running with ADB cleanly reduced to one server process and `tcp:8081 -> tcp:8081` restored.
  - Host load while the tuned emulator was running still showed memory pressure: roughly 1.2 GB available memory, about 22-50% CPU during sampled boot/settle intervals, and moderate disk activity.
- Additional system observation:
  - The largest memory consumers while the emulator was running were `qemu-system-x86_64`, `vmmemWSL`, NVIDIA Broadcast, Windows Defender, Discord, browser/WebView processes, Explorer, WhatsApp, and Teams.
  - Removed the persistent user-level `npm_config_python` environment variable that caused npm's recurring `Unknown env config "python"` warning. The current Codex shell still inherited the old variable, but new terminals should not.
- Remaining observation: Windows process command-line inspection via CIM/tasklist hung during the investigation, so unrelated low-resource Node processes were left alone unless they bound development ports.

## Manual VDSC Scrape Import

- Time: 16:03 CEST, approximate duration 8 minutes.
- User suspected Convex had stale/outdated event data and asked whether the scraping pipeline could be run manually.
- Reviewed Convex AI guidelines and the current `convex/scraping/vdsc.ts` pipeline before running it.
- Ran `npx convex run scraping/vdsc:previewEvents`; the live VDSC feed returned 57 events, with sample entries starting on 2026-06-16.
- Ran `npx convex run scraping/vdsc:importEvents '{"geocode":true}'`.
- Import result: `scraped: 57`, `stored.received: 57`, `stored.inserted: 57`, `stored.updated: 0`.
- Verified app-facing upcoming data with `npx convex run events:listUpcoming '{"limit":10}'`; it returned fresh June 16, 2026+ events with current `lastScrapedAt` timestamps.
- Note: the current import pipeline upserts feed events but does not delete older stale event documents from the database.
