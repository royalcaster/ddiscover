# 2026-05-31 Session Summary

## Context

- User asked to resume building context and requested a summary of the latest app changes plus the roadmap to MVP.
- Reviewed recent session summaries, README, recent git commits, current working tree status, and the MVP beta brief before answering.

## Findings

- Latest committed feature work includes map-first discovery, backend-synced favorites, VDSC event/club ingestion improvements, geocoding cache support, Clerk profile authentication, and Android dev tooling.
- Current working tree also contains uncommitted documentation/tooling updates around fresh-clone Android prebuild requirements and colleague setup troubleshooting.
- The existing MVP direction remains a three-screen beta: `Entdecken`, `Kalender`, and `Profil`, with cloud-synced favorites as the authenticated-only feature.
- User reported that Android Studio's emulator overlay gear in the top-right can block the app's `/profile` settings button during testing. Recommended using the emulator in a separate window or adjusting safe-area placement if the app control is too close to the physical top-right corner.
- User reported wrong map placement for Club 11. Checked the geocoding path and found that the VDSC feed provides `Club 11 e.V.\nHochschulstraße 48\n01069 Dresden`; Nominatim resolves `Club 11, Dresden, Germany` to approximately `51.0319016, 13.7307345`, but the stricter generated address query returned no rows in the check. The app then falls back to the current hardcoded `club-11` coordinate in `src/lib/club-locations.ts`, which is wrong for Club 11.

## Location MVP Decision

- User agreed that MVP should use a curated fixed-location set instead of relying on automated geocoding for map pins.
- Updated `src/lib/club-locations.ts`:
  - replaced the hidden `KNOWN_CLUB_COORDINATES` fallback with exported `MVP_CLUB_LOCATIONS` containing name, address, coordinates, and review status,
  - corrected `club-11` to `51.0319016, 13.7307345` and marked it verified,
  - fixed the `countdown` and `wu5` slug keys to match the Convex club catalog,
  - changed coordinate resolution order so MVP fixed coordinates win before stored scraped/geocoded Convex coordinates.
- Validation: `npx tsc --noEmit` passed.

## Convex Clubs Table Protection

- User entered 10 reviewed club/location datasets directly into the Convex `clubs` table and wants that table to be the MVP source without automated overwrites.
- Checked Convex write paths and found no registered `crons.ts` file. The main risk was the VDSC event import mutation patching existing `clubs` rows; additional manual-risk paths were internal maintenance mutations.
- Updated `convex/events.ts` so VDSC imports no longer patch existing club documents. Existing club rows are now treated as curated MVP data; events still link to them by slug and inherit their coordinates for event map placement.
- Updated `convex/clubs.ts` so `cleanupOrphaned` and `mergeLegacyDuplicates` skip by default and require `allowMvpClubChanges: true` before modifying/deleting clubs.
- Updated `convex/admin.ts` so `wipeAllData` preserves clubs by default and only deletes them when called with `includeClubs: true`.
- Updated `src/lib/club-locations.ts` again so the app prefers latitude/longitude from Convex `clubs` rows before using the local MVP fallback registry.
- Added a regression test in `convex/events.test.ts` proving an existing `club-11` row keeps its manually reviewed fields and coordinates during event import.
- Validation:
  - `npx tsc --noEmit` passed.
  - `npm test -- --run convex/events.test.ts` passed.
- Deployed the updated Convex functions to the configured dev deployment with `npx convex dev --once`; Convex reported functions ready at 16:16:13.

## Profile Auth Button Cleanup

- User noticed two Google sign-in buttons on the profile screen.
- Root cause: `src/app/profile.tsx` rendered the custom `GoogleSignInButton` above Clerk's native `AuthView`, while the Clerk prebuilt auth surface also renders its own Google provider button.
- MVP decision: keep the Clerk-owned button inside `AuthView` as the correct visible sign-in path and remove the custom button from the profile screen.
- Updated `src/app/profile.tsx` to render only `ClerkAuthEntry` for signed-out users.
- User still saw the top German Google button on-device; verified `profile.tsx` no longer references the custom button, indicating stale bundle or Clerk-owned UI.
- Deleted unused custom Google sign-in component files (`src/components/auth/google-sign-in-button.tsx` and `.web.tsx`) so the app source contains no `Mit Google fortfahren` button implementation.
- Restarted Expo dev server with cache clear; Metro is waiting on `http://localhost:8081`.
- Validation: `npx tsc --noEmit` passed.
- User restarted Android and still saw two Google buttons, confirming the duplicate was inside Clerk's native `AuthView`.
- Replaced the signed-out profile's native `AuthView` with a controlled MVP Google-only sign-in surface using `GoogleSignInButton`, so only one Google button is rendered.
- Re-added simplified native/web `GoogleSignInButton` components and left Clerk session activation on Clerk's backend via native Google flow plus OAuth fallback.
- Validation: `npx tsc --noEmit` passed.
- User still saw the old Clerk form after restarting. Verified active Android/Expo node processes from the previous run were still alive and that the source still contained the unused `ClerkAuthEntry` files.
- Deleted `src/components/auth/clerk-auth-entry.tsx` and `.web.tsx`, verified `rg` finds no `AuthView`, `ClerkAuthEntry`, `Continue to DDiscover`, or `Enter your email` in `src`, and force-stopped the stale DDiscover Android/Expo node processes by PID.
- Validation: `npx tsc --noEmit` passed.

## Favorites Auth Sync Fix

- User hit `Authentication required` from `favorites:toggleFavorite` after signing in and saving a club.
- Root cause: the favorites hook only checked Clerk `isSignedIn`; Convex can still be loading auth or fail to receive a Clerk JWT, so the mutation reached Convex unauthenticated.
- Updated `src/hooks/use-favorites.ts` to use `useConvexAuth()` and block favorite mutations until Convex reports authenticated. It now shows a user-facing alert instead of surfacing an uncaught Convex server error.
- Added a small signed-in profile status line showing whether Convex auth is `wird verbunden`, `verbunden`, or `nicht verbunden` to diagnose Clerk/Convex JWT handoff during testing.
- Validation: `npx tsc --noEmit` passed.
- User correctly objected that the auth-provider details should not be shown in a user-facing modal. Removed the profile Convex status line and changed favorite failure alerts to generic product language. Technical details are now emitted only to the development console via `console.warn`/`console.error`.
- User still saw `Clerk signed in but Convex auth is not authenticated`.
- Verified `.env.local` Clerk publishable key decodes to `new-krill-96.clerk.accounts.dev`, matching `convex/auth.config.ts`, so the issuer domain was correct.
- Used Clerk CLI to inspect JWT templates: `bunx clerk api /jwt_templates --instance dev` returned `[]`, meaning the required `convex` JWT template was missing.
- Created `docs/clerk-jwt-template-convex-dev.json` and applied it with `bunx clerk api /jwt_templates --instance dev --method POST --file docs/clerk-jwt-template-convex-dev.json --yes`.
- Clerk created template `jtmp_3EUoEODUgtVbntgKHjSsQPkAmkS` with `name: "convex"`, `claims.aud: "convex"`, RS256 signing, lifetime 3600, clock skew 5.
- Re-ran `npx convex dev --once`; Convex reported functions ready at 17:46:48.
- Validation: `npx tsc --noEmit` passed.

## Signed-In Profile Enhancement

- User confirmed favorites auth works and asked to enhance the signed-in profile page with the Google profile picture and a Favorites section.
- Updated `src/app/profile.tsx`:
  - signed-in profile card now shows `user.imageUrl` from Clerk/Google with a fallback shield icon,
  - displays the user's name and primary email,
  - adds a `Favorites` section below the profile card,
  - favorites section reads `useFavorites()`, `api.clubs.list`, and `api.events.listUpcoming`,
  - shows favorite count, loading/empty states, and up to five favorite clubs/events.
- Validation: `npx tsc --noEmit` passed.
- User reported no profile picture was visible on Android. Updated the image rendering to use explicit React Native `StyleSheet` dimensions (`56x56`, circular radius) instead of relying on NativeWind class sizing for `expo-image`, and added `onError` fallback to the shield icon.
- Validation: `npx tsc --noEmit` passed.

## Calendar Design Pass

- User asked to make the calendar screen look more like the initial dark mockup.
- Rebuilt `src/app/calendar.tsx` away from the generic `ScreenShell` card layout into a custom dark calendar screen:
  - black background,
  - large `Kalender` title,
  - compact seven-day selector with yellow selected day,
  - dark `Alle Clubs` filter bar,
  - dense event list container with dividers,
  - thumbnail-style event rows using the existing `logo-glow.png` asset,
  - event time, title, club, inferred genre text, and heart favorite action.
- Kept real Convex event/club data and favorite toggling.
- Validation: `npx tsc --noEmit` passed.
- User objected that the calendar was hardcoded dark and asked for light/dark/auto support from the profile settings flow.
- Confirmed a settings screen already exists behind the profile gear with `ThemeModeToggle`.
- Updated `src/app/calendar.tsx` to use semantic theme classes/colors instead of hardcoded dark colors: `bg-background`, `bg-card`, `border-border`, `text-foreground`, `text-muted-foreground`, and `theme.primary`/`theme.mutedForeground` for icons.
- Updated `src/components/theme-mode-toggle.tsx` to label the system option as `Auto`.
- Validation: `npx tsc --noEmit` passed.
- User asked for the calendar day strip to always show the real seven-day window starting today, with the first item labeled as today. Current session date is Sunday, May 31, 2026.
- Updated `src/app/calendar.tsx` so the day strip is generated from local start-of-today plus the next six days, independent of event availability. The first day label now renders `Heute`; remaining days render weekday abbreviations and real day numbers.
- Validation: `npx tsc --noEmit` passed.
- User asked to remove the unused `Alle Clubs` dropdown and reported the settings screen was not showing.
- Removed the calendar filter bar and unused chevron import from `src/app/calendar.tsx`.
- Added nested route `src/app/profile/settings.tsx` re-exporting the existing settings screen and changed the profile gear navigation from `/settings` to `/profile/settings`, so settings opens inside the profile tab route hierarchy.
- Validation: `npx tsc --noEmit` passed.
- User reported settings still did not work. Replaced settings routing with an in-tab profile subview: the profile gear now toggles the profile screen between `Profil` and `Einstellungen`, and the header icon changes to a back arrow while settings is visible. This avoids native-tab route limitations.
- Validation: `npx tsc --noEmit` passed.

## Map Provider Switch

- User asked whether the app can use a lighter and more stylable map provider than Google Maps, including dark mode support.
- Added `@maplibre/maplibre-react-native` and replaced the native Discover map implementation with MapLibre's `Map`, `Camera`, and `Marker` components.
- Removed the Google Maps API-key path from `app.config.ts` and now inject `@maplibre/maplibre-react-native` as the native Expo plugin.
- Removed the `react-native-maps` runtime dependency from `package.json`, `package-lock.json`, and `bun.lock`.
- Added shared `MapRegion` typing in `src/lib/map-types.ts` so the rest of the Discover screen does not depend on `react-native-maps` types.
- The Discover map now uses CARTO Positron for light mode and CARTO Dark Matter for dark mode through MapLibre style URLs, following the existing app theme setting.
- Validation: `npx tsc --noEmit` passed.
