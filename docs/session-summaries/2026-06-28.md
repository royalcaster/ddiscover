# 2026-06-28

## Calendar Swipe Navigation

- Time: 12:16 CEST, approximate duration 20 minutes.
- User asked for the calendar screen to support horizontal swiping through days and for the selected day background in the top strip to animate smoothly.
- Reviewed existing session summaries before changing behavior, especially the prior calendar design notes from 2026-05-31 and Android interaction notes from 2026-06-24/25.
- Updated `src/app/(tabs)/calendar.tsx`:
  - added a horizontal `PanResponder` to the calendar scroll area so swiping left/right selects the next/previous day while vertical scrolling remains available,
  - replaced the selected day chip's static background with a measured `Animated.View` highlight that slides and resizes to the selected day,
  - kept the existing seven-day window, event filtering, favorites, and event-detail navigation behavior,
  - changed day-key generation to use local calendar dates instead of UTC ISO slicing so the visible day and filtered events align in the local timezone.
- Validation:
  - `npx tsc --noEmit` passed,
  - `npm run lint` passed.

## Favorites Re-Enablement

- Time: 12:20 CEST, approximate duration 15 minutes.
- User reported that after signing in through the profile screen, saving a club still showed the temporary alert: "Speichern noch nicht möglich - Favoriten brauchen noch die Convex-Anbindung an Clerk."
- Reviewed existing session summaries and Convex AI guidelines before changing the auth/favorites path.
- Root cause: `src/hooks/use-favorites.ts` had intentionally disabled all signed-in favorite mutations during the earlier standalone APK Convex WebSocket investigation. It returned empty favorite sets and always showed the temporary alert for signed-in users.
- Restored favorites without reintroducing realtime WebSocket dependency:
  - `useFavorites` now gets Clerk's `convex` JWT template token with `getToken({ template: 'convex' })`,
  - uses `ConvexHttpClient` with that JWT for `api.favorites.listMyFavorites` and `api.favorites.toggleFavorite`,
  - refreshes favorite IDs after sign-in and updates local favorite state after save/unsave,
  - keeps the signed-out sign-in prompt and uses generic user-facing error text while logging technical failures to the console.
- The existing Convex favorites backend and tests were still valid; the break was in the app hook, not the Convex schema or mutation logic.
- Tried to verify the live Clerk JWT template with `bunx clerk api /jwt_templates --instance dev`, but the shell had no Clerk secret key or linked project context available.
- Validation:
  - `npx tsc --noEmit` passed,
  - `npm run lint` passed,
  - `npm test -- --run convex/favorites.test.ts` passed,
  - `npm test -- --run` passed: 6 files, 19 tests.

## Discover Drawer Dark Icon Fix

- Time: 12:22 CEST, approximate duration 3 minutes.
- User reported that the `Details` and `Quelle` buttons in the map drawer used black icons in dark mode.
- Fixed `src/components/discover-bottom-sheet.tsx` by giving the two Lucide icons explicit `colors.foreground` strokes, matching the surrounding button text and theme.
- Validation:
  - `npx tsc --noEmit` passed,
  - `npm run lint` passed,
  - `git diff --check -- src/components/discover-bottom-sheet.tsx` passed.

## Clerk Workspace Relink Verification

- Time: 13:10 CEST, approximate duration 5 minutes.
- User relinked the Clerk CLI from the wrong workspace to the DDiscover workspace and asked to check again.
- Verified from the repository root:
  - `bunx clerk whoami` reports the expected user account,
  - `bunx clerk doctor --spotlight` now sees development instance `ins_3CnSds4zdDN3DWtzwkwAB2ENkn6`,
  - `bunx clerk apps list` shows application `DDiscover` with application id `app_3CnSdrbTw2W8RAw4F7Czle5rzsv`,
  - `bunx clerk api /jwt_templates --instance dev` returns the required `convex` JWT template.
- Confirmed the Clerk JWT template has `claims.aud: "convex"`, lifetime `3600`, allowed clock skew `5`, and RS256 signing.
- Confirmed local `convex/auth.config.ts` matches the linked Clerk instance domain and Convex audience/application id.

## Favorites JWT Verification Follow-Up

- Time: 13:16 CEST, approximate duration 5 minutes.
- User asked whether the earlier favorites fix had actually been checked after the Clerk workspace was relinked.
- Rechecked the full static/auth path:
  - `src/hooks/use-favorites.ts` requests `getToken({ template: 'convex' })`,
  - the linked Clerk development instance has a `convex` JWT template with audience `convex`,
  - `convex/auth.config.ts` uses the same Clerk domain and `applicationID: 'convex'`,
  - `convex/favorites.ts` still gates mutations through `ctx.auth.getUserIdentity()` and user `tokenIdentifier`.
- Validation:
  - `npm test -- --run convex/favorites.test.ts` passed: 1 file, 3 tests,
  - `npx tsc --noEmit` passed,
  - `npm run lint` passed.
- Remaining limitation: this verifies configuration and backend behavior, but a real signed-in mobile session still needs an on-device smoke test to prove Clerk's runtime `getToken` returns the template JWT inside the APK/dev build.
