# 2026-06-30

## Remote Sync Check

- Time: 11:31 CEST, approximate duration 10 minutes.
- User asked to first check whether the GitLab remote and GitHub remote are in sync.
- Reviewed existing session summaries before checking remotes, as required by `AGENTS.md`.
- Current `.git/config` only contains `origin`, pointing to `https://github.com/royalcaster/ddiscover.git`.
- Prior summaries show a former `gitlab` remote at `https://iversion.informatik.htw-dresden.de/mobile-networks-ddisocver/ddiscover.git`, but that remote is no longer configured in this checkout.
- Ran `git fetch origin --prune`; GitHub `origin/main` advanced from `6b0bf76` to `2d36c81`.
- Local `main` is behind GitHub `origin/main` by 1 commit:
  - local `main`: `6b0bf76150c18f9ed11d207109407d07fca95992` (`docs(session): record final github push`)
  - GitHub `origin/main`: `2d36c818611be29c1489bf15c24aa164c32fbf04` (`fix(calendar): restore static day interactions`)
- Attempted to query the recorded GitLab URL with `GIT_TERMINAL_PROMPT=0 git ls-remote`, but the HTW GitLab server required credentials and the shell has no usable non-interactive credential helper for that URL.
- Conclusion: the GitLab/GitHub sync state cannot be confirmed from this shell until the GitLab remote and credentials are restored. The local checkout is currently configured only for GitHub and is one commit behind GitHub `main`.
- User provided the HTW GitLab URL again. Restored it as the local `gitlab` remote.
- `git fetch gitlab --prune` still failed because the server requires a username/password or token and no credential helper is configured in this shell.
- Current remotes:
  - `origin`: `https://github.com/royalcaster/ddiscover.git`
  - `gitlab`: `https://iversion.informatik.htw-dresden.de/mobile-networks-ddisocver/ddiscover.git`
- User provided a GitLab access token for the HTW GitLab repository.
- Used the token only for transient Git authentication and fetched `gitlab/main` successfully.
- Remote comparison showed GitLab was ahead:
  - local `main`: `6b0bf76150c18f9ed11d207109407d07fca95992`
  - GitHub `origin/main`: `2d36c818611be29c1489bf15c24aa164c32fbf04`
  - GitLab `gitlab/main`: `2ad19572f71eae304794d1989b5d53c14ae1da56`
- `origin/main...gitlab/main` was `0 1`, meaning GitHub had no unique commits and GitLab had one unique commit.
- The GitLab-only commit was `2ad1957 docs(submission): finalize learning journal materials`.
- Synced GitHub by fast-forward pushing `gitlab/main` to `origin/main`.
- Re-fetched GitHub and verified `origin/main...gitlab/main` is now `0 0`.
- Fast-forwarded local `main` to `2ad1957` as well, so local, GitHub, and GitLab now point to the same commit.

## General Nightlife Ingestion Review Pipeline

- Time: 12:28 CEST, approximate duration 65 minutes.
- User asked to implement the plan for a more stubborn and reliable scraping system using n8n, LLM extraction, Google Places data, Convex validation, and a review workflow.
- User also instructed that GitLab must no longer be pushed to and GitHub should be used moving forward.
- Reviewed existing session summaries and Convex AI guidelines before changing Convex code.
- Implemented the first production-shaped ingestion slice:
  - added Convex candidate tables for ingestion sources, venue candidates, and event candidates,
  - added Google Places metadata fields to public `clubs`,
  - added ticket/tag/confidence metadata to public `events`,
  - added `convex/ingestion.ts` with candidate ingestion, admin review listing, venue approval, event approval, and rejection/recheck status mutations,
  - added `convex/http.ts` with `POST /ingestion/candidates`, protected by `DDISCOVER_INGESTION_TOKEN`,
  - added a private desktop review route at `/admin/ingestion`,
  - added `docs/ingestion-pipeline.md` documenting the n8n payload, required Convex env vars, and review flow,
  - updated `README.md` and `AGENTS.md` with the ingestion pipeline and GitHub-only push policy,
  - updated `convex/admin.ts` so full data wipes also clear ingestion candidate tables.
- Review access is controlled server-side through `DDISCOVER_ADMIN_EMAILS`; admins must be signed in through Clerk and receive a Convex JWT.
- Removed the local `gitlab` remote from this checkout so only GitHub `origin` remains configured.
- Could not run `npx convex codegen` because this shell has no `CONVEX_DEPLOYMENT` configured and no `.env.local`/`.env` file is present. Manually updated `convex/_generated/api.d.ts` so TypeScript can see the new `ingestion` module; rerun codegen once the deployment env is available.
- Validation:
  - `npx tsc --noEmit` passed,
  - `npm test -- --run convex/ingestion.test.ts` passed: 1 file, 4 tests,
  - `npm test -- --run` passed: 7 files, 23 tests,
  - `npm run lint` passed.

## Environment Example Refresh

- Time: 12:35 CEST, approximate duration 5 minutes.
- User asked to create a `.env.example`.
- Found an existing minimal `.env.example` with only the four Expo public variables.
- Expanded it with placeholders and comments for:
  - `CONVEX_DEPLOYMENT`,
  - `EXPO_PUBLIC_CONVEX_URL`,
  - Clerk public and Google OAuth client IDs,
  - ingestion/admin review variables `DDISCOVER_INGESTION_TOKEN` and `DDISCOVER_ADMIN_EMAILS`,
  - optional scraper tuning variables,
  - optional Android helper script variables.
- No real secrets were added.

## n8n Workflow Exports

- Time: 12:45 CEST, approximate duration 15 minutes.
- User clarified that n8n workflows should live in the monorepo and asked whether a future GitHub Action can push them to n8n Cloud or a custom/self-hosted instance.
- Added `n8n/workflows/ddiscover-webpage-llm-ingestion.json`:
  - manual and 12-hour scheduled trigger,
  - source catalog from `DDISCOVER_N8N_SOURCES_JSON` with a Dresden fallback source,
  - page fetch and HTML cleanup,
  - OpenAI JSON-schema extraction for venue/event candidates,
  - normalization into the Convex ingestion payload,
  - POST to Convex `/ingestion/candidates`.
- Added `n8n/workflows/ddiscover-google-places-bootstrap.json`:
  - manual trigger,
  - Google Places Text Search query list from `DDISCOVER_GOOGLE_PLACES_QUERIES_JSON`,
  - conversion of Places results into venue candidates,
  - POST to Convex `/ingestion/candidates`.
- Added `docs/n8n-workflows.md` with required n8n environment variables, source/query examples, and future GitHub Actions deployment guidance through the n8n public API.
- Updated `.env.example` with n8n/OpenAI/Google Places variables and updated `README.md` with a pointer to the workflow exports.
- Validation:
  - parsed both workflow JSON files with Node successfully,
  - `git diff --check -- .env.example README.md docs/n8n-workflows.md n8n/workflows` passed.

## Raspberry Pi n8n Self-Hosting Setup

- Time: 13:00 CEST, approximate duration 20 minutes.
- User said they will host the n8n workflow on a Raspberry Pi 5 and asked to prepare what is necessary.
- Added `n8n/self-host/raspberry-pi/docker-compose.yml`:
  - n8n container,
  - PostgreSQL 16 Alpine container,
  - persistent Docker volumes for n8n and Postgres,
  - healthcheck-based startup ordering,
  - DDiscover/OpenAI/Google Places environment variables passed into n8n,
  - `N8N_BLOCK_ENV_ACCESS_IN_NODE=false` so workflow Code nodes can read `process.env`.
- Added `n8n/self-host/raspberry-pi/.env.example` with Pi/LAN defaults, generated-key instructions, Convex site URL placeholder, ingestion token placeholder, OpenAI key, Google Places key, source JSON, and Places query JSON.
- Updated `.gitignore` so `n8n/self-host/**/.env` is ignored.
- Added `docs/n8n-raspberry-pi.md` with first setup, Convex env requirements, security notes, operations commands, backups, and future GitHub Actions deployment path.
- Updated the n8n workflow JSON files to read environment variables through `process.env` inside Code nodes.
- Validation:
  - parsed both n8n workflow JSON files with Node successfully,
  - `docker compose -f n8n/self-host/raspberry-pi/docker-compose.yml --env-file n8n/self-host/raspberry-pi/.env.example config` passed,
  - `git diff --check -- .gitignore README.md .env.example docs/n8n-workflows.md docs/n8n-raspberry-pi.md n8n` passed.

## GitHub Commit Preparation

- Time: 12:58 CEST, approximate duration 8 minutes.
- User asked to commit everything and push to GitHub.
- Confirmed only GitHub `origin` is configured as a remote.
- Final validation before commit:
  - `npx tsc --noEmit` passed,
  - `npm test -- --run` passed: 7 files, 23 tests,
  - `npm run lint` passed,
  - both n8n workflow JSON files parsed successfully,
  - Raspberry Pi n8n Docker Compose config rendered successfully,
  - `git diff --check` passed.
- Tried `npx convex codegen` again before commit; it still failed with `No CONVEX_DEPLOYMENT set`, so generated API types remain manually updated for the new `ingestion` module until codegen can run in a Convex-configured shell.
