# 2026-06-25

## Map Drawer and Emulator Verification Follow-Up

- Time: 01:35-02:10 CEST, approximate duration 35 minutes.
- Continued fixing regressions from the Android map UI polish pass.
- Verified the latest GitHub Actions release APK cannot be used on the x86_64 emulator because the release build lacks emulator-compatible native libraries. Local emulator screenshots must use the debug/dev build.
- Recovered the local Android dev loop:
  - restarted Metro with a clean cache,
  - used the repo Android dev workflow to reinstall/start the development build,
  - restarted `adb.exe` after it hung during screenshot capture.
- Emulator screenshots were captured in `.artifacts/screenshots/` for local inspection. These are intentionally not committed.
- Confirmed the drawer can now be dragged from the sheet content, not only from the handle:
  - collapsed state,
  - half state,
  - expanded state.
- Confirmed German umlauts render correctly in the current dev build; earlier mojibake screenshots were from an older build/state.
- Fixed remaining map and theme issues:
  - MapLibre marker layer colors now use concrete hex/RGBA values instead of app `hsl(...)` theme strings, because MapLibre rejected those color expressions.
  - Club marker icons now use a registered MapLibre image layer and a larger `icon-size`, making the markers readable again while keeping native map-layer performance.
  - Map controls remain stacked on the right and use Android ripple feedback.
  - Active bottom tabs no longer use the yellow primary color; they use a subtle themed secondary surface with normal foreground icons/text, avoiding yellow-on-white or low-contrast icon states.
  - Favorited club hearts in the map drawer no longer become yellow icons on a white/light surface.
- Validation:
  - `npx tsc --noEmit` passed,
  - `npm run lint` passed,
  - `npm test -- --run` passed: 6 files, 17 tests,
  - `git diff --check` passed.
- Note: the Expo dev-client overlay gear appears in emulator screenshots only for local debug builds and will not appear in the GitHub Actions release APK.
